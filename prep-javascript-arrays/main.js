const languages = ['html', 'css', 'javascript', 'react', 'node', 'postgres'];
console.log(languages);

var firstElement = languages[0];
console.log('The first element of this array is:', firstElement);

var thirdElement = languages[3];
console.log('The third element of this array is:', thirdElement);

var length = languages;
console.log('The length of the array is:', length.length);

var lastElement = languages;
console.log('The last element of the array is:', languages[lastElement.length - 1]);
